
const template = "export function handle<FN_NAME_UC>(call: <FN_NAME_UC>Call) {paramHandler(call, \"<FN_NAME_LC>\")}"

const args = process.argv.slice(2)

function applyTemplate(name){
    
}

applyTemplate()
